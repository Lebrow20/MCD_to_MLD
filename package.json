{
  "name": "mcd-to-mld",
  "private": true,
  "workspaces": {
    "packages": ["backend", "frontend", "shared"]
  },
  "scripts": {
    "install:clean": "rm -rf node_modules package-lock.json && npm install",
    "dev": "npm run dev -w @mcd-to-mld/backend & npm run dev -w @mcd-to-mld/frontend",
    "build": "npm run build -w @mcd-to-mld/shared && npm run build -w @mcd-to-mld/backend && npm run build -w @mcd-to-mld/frontend"
  }
}